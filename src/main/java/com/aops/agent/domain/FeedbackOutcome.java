package com.aops.agent.domain;

/**
 * Human feedback outcome for a case (the feedback flywheel).
 */
public enum FeedbackOutcome {
    TRUE_POSITIVE,
    FALSE_POSITIVE,
    RESOLVED,
    UNCERTAIN
}
