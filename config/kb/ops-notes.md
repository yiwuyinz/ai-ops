# 运维知识库示例

## 常见告警处理速查

- **DemoAppDown**：Prometheus 抓取失败，先看目标服务是否存活（详见 runbook: demo-app-down）。
- **HighErrorRate**：先查错误日志（`|= "error"`），再对时间线确认是否伴随部署。
- **CPU 高**：先查指标确认是哪个容器，再查该容器日志找原因。

## 值班须知

- 所有调查过程都会记录证据链（谁查了什么、结果如何），最终报告可在 /api/cases/{id} 查看。
- 误报请点击反馈按钮标注，系统会按告警规则统计误报率。
