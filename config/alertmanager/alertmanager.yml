route:
  group_by: ["alertname"]
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1h
  receiver: aops-agent

receivers:
  - name: aops-agent
    webhook_configs:
      - url: "http://host.docker.internal:8080/api/alerts"
        send_resolved: false
